pipeline{
    agent any

    stages{
        stage('Deploy') {
            steps {
                sh '''
                    oc project scbaof-deploy-strategies
                    oc start-build greeting-service --follow --wait
                '''
            }
        }
    }
}
